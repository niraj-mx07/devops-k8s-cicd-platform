apiVersion: v1
kind: Service
metadata:
  name: devops-task-api
  namespace: staging
  labels:
    app: devops-task-api
    environment: staging

spec:
  type: NodePort

  selector:
    app: devops-task-api

  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
